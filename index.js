"// Luxury Real Estate is a pure HTML/CSS/JS static site served from /public"
// React entry intentionally does nothing to avoid hijacking the static pages.
// (Required because the platform's create-react-app build is the default dev server.)
const root = document.getElementById('root');
if (root) root.remove();